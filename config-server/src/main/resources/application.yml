server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rahulmali123/dbs-config-repo
          default-label: master
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: "*"